# JWT secret (HS512 requires 64+ bytes)
jwt.secret=test-secret-key-that-is-definitely-long-enough-for-hs512-algorithm-at-least-64b

# Mail (test stub values — no actual mail sent in tests)
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=test@test.com
spring.mail.password=test

# Frontend URL
app.frontend.url=http://localhost:3000

# Disable repopulation in tests
app.database.repopulate=false
